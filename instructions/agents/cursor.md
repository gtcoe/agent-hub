# Cursor — Agent Notes

## Instruction Source

This rules file is generated by `.agent-hub/bin/agent-sync.js`. Do not edit `.cursor/rules/dotpe-rules.md` directly.
To change instructions: edit sources in `.agent-hub/instructions/`, then run `node .agent-hub/bin/agent-sync.js`.

## MCP Servers

MCP servers are configured in `~/.cursor/mcp.json`. All MCPs route through `.agent-hub/bin/agent-mcp.js`
which resolves secrets at runtime from `.agent-hub/secrets/.env`.

## Skills

Available skills are embedded below. Invoke them when the user requests the skill by name or trigger phrase.

# GitHub Copilot — Agent Notes

## Instruction Source

This file is generated by `.agent-hub/bin/agent-sync.js`. Do not edit `.github/copilot-instructions.md` directly.
To change instructions: edit sources in `.agent-hub/instructions/`, then run `node .agent-hub/bin/agent-sync.js`.

## Scoped Instructions

Per-file-type instructions are no longer in `.github/instructions/*.instructions.md` (that directory is now legacy).
All rules are consolidated here. The `applyTo` frontmatter has been removed; rules apply globally by default.

## Skills

Skills are available in `.github/skills/` (symlinks managed by `node .agent-hub/bin/agent-install.js --skills --agent github-copilot`).
